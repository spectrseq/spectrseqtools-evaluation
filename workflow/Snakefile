configfile: "config/config.yaml"


include: "rules/common.smk"
include: "rules/simulation.smk"
include: "rules/prediction.smk"


rule all:
    input:
        collect(
            [
                "results/datavzrd/{seq}/{n_fragments}",
                # TODO implement proper integration with dev branch of lionelmssq
                #"results/lionelmssq/{seq}/{n_fragments}.fasta",
            ],
            seq=lookup(dpath="simulation/seqs", within=config),
            n_fragments=lookup(dpath="simulation/n_fragments", within=config),
        ),